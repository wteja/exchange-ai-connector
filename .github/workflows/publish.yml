name: Publish to PyPI

# Publishes on a GitHub Release (tag like v0.2.0). Uses PyPI Trusted Publishing
# (OIDC) — no API token secret to manage. One-time setup: on PyPI, add a
# trusted publisher for this repo (workflow: publish.yml, environment: pypi).
# https://docs.pypi.org/trusted-publishers/

on:
  release:
    types: [published]

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write  # required for trusted publishing
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade build
          python -m build
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
